/**
 * The Loader function returns a div element with the class name "loader".
 * @returns A JSX element representing a loader with the class name "loader" is being returned.
 */
function Loader() {
  return <div className="loader"></div>;
}

export default Loader;
